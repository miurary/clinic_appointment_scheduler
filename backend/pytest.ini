[pytest]
DJANGO_SETTINGS_MODULE = config.settings.dev
python_files = test_*.py
testpaths = apps

# --reuse-db keeps the test database between runs. After changing a migration,
# run once with --create-db or you will test against a stale schema.
addopts = -q --reuse-db
