# Local + CI only. Kept out of the production image.
-r requirements.txt

pytest==9.1.1
pytest-django==4.12.0

# Freezes the clock. Slot generation reads timezone.now(), so without this the
# suite behaves differently depending on the day it happens to run.
time-machine==3.2.0
